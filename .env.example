###############################################
# ThingsBoard connection
###############################################

# REQUIRED: ThingsBoard host to connect to (hostname or IP)
# Example: TB_HOST=yoursubdomain.your-domain.com
TB_HOST=

# REQUIRED: ThingsBoard MQTT TLS port (TLS is always enabled by the gateway)
# Example: TB_PORT=8883
TB_PORT=

###############################################
# Provisioning and device identity
###############################################

# Self-provisioning credentials (from ThingsBoard provisioning settings)
THINGSBOARD_PROVISION_DEVICE_KEY=
THINGSBOARD_PROVISION_DEVICE_SECRET=

# Optional explicit device name. Defaults to OS hostname if unset.
# Example: THINGSBOARD_DEVICE_NAME=$(hostname)
THINGSBOARD_DEVICE_NAME=

# Optional: Path to a custom CA certificate used for TLS verification.
# Example: THINGSBOARD_CA_CERT=/etc/ssl/certs/custom_ca.pem
THINGSBOARD_CA_CERT=

# Optional: File path where the access token is stored/read.
# If the file does not exist yet, the gateway will self-provision and
# write the newly obtained token to this path.
# Default: ./tb_access_token (relative to working directory)
# Example: THINGSBOARD_ACCESS_TOKEN=/home/pi/thingsboard-edge-gateway/tb_access_token
THINGSBOARD_ACCESS_TOKEN=

###############################################
# Filesystem paths (see src/utils/paths.py)
###############################################

# Base data directory for the Gateway (DBs, buffers, etc.).
# Default: project directory
# Example: TEG_DATA_PATH=/home/pi/thingsboard-edge-gateway/data
TEG_DATA_PATH=

# Optional: Base data directory for the controller software.
# Default: falls back to TEG_DATA_PATH
# Example: TEG_CONTROLLER_DATA_PATH=/home/pi/thingsboard-edge-gateway/controller_data
TEG_CONTROLLER_DATA_PATH=

# Optional: Directory for controller log files.
# Default: <TEG_CONTROLLER_DATA_PATH>/logs
# Example: TEG_CONTROLLER_LOGS_PATH=/home/pi/thingsboard-edge-gateway/controller_logs
TEG_CONTROLLER_LOGS_PATH=

# REQUIRED: Path to the controller Git repository .git directory.
# Used for OTA/update operations. If unset, startup logs a fatal error.
# Example: TEG_CONTROLLER_GIT_PATH=/home/pi/thingsboard-edge-gateway/controller/.git
TEG_CONTROLLER_GIT_PATH=

# Optional: Docker build context for the controller image.
# Default: <dirname(TEG_CONTROLLER_GIT_PATH)>/docker
# Example: TEG_CONTROLLER_DOCKERCONTEXT_PATH=/home/pi/thingsboard-edge-gateway/controller/docker
TEG_CONTROLLER_DOCKERCONTEXT_PATH=

# Optional: Path (relative to Docker context) to the controller Dockerfile.
# Default: ./Dockerfile
# Example: TEG_CONTROLLER_DOCKERFILE_PATH=./Dockerfile
TEG_CONTROLLER_DOCKERFILE_PATH=

# Optional: Default controller version (Git tag or commit hash) to use if
# the last-launched version file is not present.
# Example: TEG_DEFAULT_CONTROLLER_VERSION=v1.2.3
TEG_DEFAULT_CONTROLLER_VERSION=

###############################################
# Runner (used by scripts/setup_and_run_gateway_process.sh)
###############################################

# Required only when running via the runner script: absolute path to the
# gateway project directory on the host.
# Example: TEG_GATEWAY_DIR=/home/pi/thingsboard-edge-gateway
TEG_GATEWAY_DIR=