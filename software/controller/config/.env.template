# these variables have to be defined in the environment
# and are not configurable via the server but only via
# direct (terminal) access

ACROPOLIS_HARDWARE_LOCKFILE_PATH=""
ACROPOLIS_MODE="simulate"